<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.Odev" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/surface_dark</item>
        <item name="colorPrimaryDark">@color/bg_dark</item>
        <item name="colorAccent">@color/accent_yellow</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <style name="Theme.Odev" parent="Base.Theme.Odev" />

    <!-- Toolbar yazi rengini beyaz tutmak icin -->
    <style name="ToolbarTitle">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
